{
  "name": "cogniflow-fullstack",
  "private": true,
  "version": "1.0.0",
  "description": "CogniFlow Fullstack (Frontend + Backend)",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}